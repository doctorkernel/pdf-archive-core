# PDF Archive Core

Shared library for:

- extracting text from PDFs locally
- batching LM Studio metadata extraction requests
- determining document date priority
- inferring vendor, title, and category
- building normalized archive output paths

This package is intended to be used by:

- `gmail-pdf-archiver`
- `pdf-scan-organizer`
